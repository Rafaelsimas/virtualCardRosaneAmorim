const openPixKey = () => {
  const openCloseCard  = document.querySelector('.open-close-card ')
  openCloseCard.classList.remove('escondido')
}

const btnCloseCardPix = () => {
  const openCloseCard  = document.querySelector('.open-close-card ')
  openCloseCard.classList.add('escondido')
}